[project]
name = "jellyfin-music-replay-playlist-generator"
version = "0.1.0"
description = "Generate music replay playlists for Jellyfin, based on the database generated by the Playback Reporting plugin."
readme = "README.md"
requires-python = ">=3.14"
dependencies = []
